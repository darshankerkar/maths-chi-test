# Chi-Square AI Impact (Frontend)

React + Vite UI for entering a contingency table and calling the backend:
- `POST /api/chi-square`

## Run

```powershell
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`.

